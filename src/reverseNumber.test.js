import { reverseNumber } from "./reverseNumber";

 describe("reverseNumber", () => {
  it("reverses the number 12345", () => {
    expect(reverseNumber(12345)).toBe(54321); // TODO Your test instead
  });
  
  it("reverses the number 83231", () => {
    expect(reverseNumber(83231)).toBe(13238); // TODO Your test instead
  });
});