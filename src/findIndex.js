export const findIndex = () => {
  // findIndex(array, predicate)
  // returns the first index where the predicate truth test passes;
  // otherwise returns -1.
  // See the test to get some ideia of what the function has to do
};