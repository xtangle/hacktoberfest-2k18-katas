import { smallestNumberInArray } from './smallestNumberInArray';

/*
 * Example of the assertions you can use for your tests:
 *
 * - Testing equality between the function's output and a constant:
 * expect(smallestNumberInArray()).toEqual(123);
 *
 * - Testing that the function returns null:
 * expect(smallestNumberInArray()).toBeNull();
 *
 * - Testing that the function returns a falsy value (eg. false, 0, "")
 * expect(smallestNumberInArray()).toBeFalsy();
 *
 * - Testing that the function returns a truthy value (eg. true, 1, "abc")
 * expect(smallestNumberInArray()).toBeTruthy();
 *
 * - Testing that the function throws
 * expect(() => { smallestNumberInArray(); }).toThrow();
 */

describe('smallestNumberInArray', () => {
  it('smallestNumberInArray does this thing...', () => {
    // TODO Your own assertion here
  });

  it('smallestNumberInArray does that other thing...', () => {
    // TODO Your own assertion here
  });

  it('smallestNumberInArray does a very cool thing...', () => {
    // TODO Your own assertion here
  });
});
