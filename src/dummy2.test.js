import { dummy2 } from './dummy2';

describe('dummy2', () => {
  it('does something', () => {
    expect(dummy2(1)).toEqual(-1);
  });

  it('does something else', () => {
    expect(dummy2(2)).toEqual(-2);
  });
})
