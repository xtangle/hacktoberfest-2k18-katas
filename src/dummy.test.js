import { dummy } from './dummy';

describe('dummy', () => {
  it('does something', () => {
    expect(dummy(1)).toEqual(1);
  });

  it('does something else', () => {
    expect(dummy(2)).toEqual(2);
  });
});
