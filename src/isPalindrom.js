export const isPalindrom = (word) => {
  // The function should return true or false if a
  // word is a palindrom or not

  // A palindrom is a word the means backwards the
  // same as foreward (e.g. Anna)
};
