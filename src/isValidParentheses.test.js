import { isValidParentheses } from './isValidParentheses';

/*
 * Example of the assertions you can use for your tests:
 *
 * - Testing equality between the function's output and a constant:
 * expect(isValidParentheses()).toEqual(123);
 *
 * - Testing that the function returns null:
 * expect(isValidParentheses()).toBeNull();
 *
 * - Testing that the function returns a falsy value (eg. false, 0, "")
 * expect(isValidParentheses()).toBeFalsy();
 *
 * - Testing that the function returns a truthy value (eg. true, 1, "abc")
 * expect(isValidParentheses()).toBeTruthy();
 *
 * - Testing that the function throws
 * expect(() => { isValidParentheses(); }).toThrow();
 */

describe('isValidParentheses', () => {
  it('isValidParentheses does this thing...', () => {
    // TODO Your own assertion here
  });

  it('isValidParentheses does that other thing...', () => {
    // TODO Your own assertion here
  });

  it('isValidParentheses does a very cool thing...', () => {
    // TODO Your own assertion here
  });
});
