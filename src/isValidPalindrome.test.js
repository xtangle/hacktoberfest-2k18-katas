import { isValidPalindrome } from './isValidPalindrome';

/*
 * Example of the assertions you can use for your tests:
 *
 * - Testing equality between the function's output and a constant:
 * expect(isValidPalindrome()).toEqual(123);
 *
 * - Testing that the function returns null:
 * expect(isValidPalindrome()).toBeNull();
 *
 * - Testing that the function returns a falsy value (eg. false, 0, "")
 * expect(isValidPalindrome()).toBeFalsy();
 *
 * - Testing that the function returns a truthy value (eg. true, 1, "abc")
 * expect(isValidPalindrome()).toBeTruthy();
 *
 * - Testing that the function throws
 * expect(() => { isValidPalindrome(); }).toThrow();
 */

describe('isValidPalindrome', () => {
  it('isValidPalindrome does this thing...', () => {
    // TODO Your own assertion here
  });

  it('isValidPalindrome does that other thing...', () => {
    // TODO Your own assertion here
  });

  it('isValidPalindrome does a very cool thing...', () => {
    // TODO Your own assertion here
  });
});
